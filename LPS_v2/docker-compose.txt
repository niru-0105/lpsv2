docker-compose up
    Description: Start up the services defined in your docker-compose.yml file.

docker-compose stop
    Description: Stop the services defined in your docker-compose.yml file without removing them.
    
docker-compose start
    Description: Start the services that were stopped by docker-compose stop.

docker-compose down
    Description: Stop and remove all containers defined in your docker-compose.yml file.